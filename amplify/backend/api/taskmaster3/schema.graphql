type TaskGenerated @model {
  id: ID!
  title: String!
  body: String
  state: String
  Team: team @connection(name: "task")
}

type Team @model {
  if: ID!
  name: String!
  task: [Task] @connection(name: "task")
}
